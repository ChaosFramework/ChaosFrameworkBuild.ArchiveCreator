<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import
    Project="$(MSBuildExtensionsPath)/$(MSBuildToolsVersion)/Microsoft.Common.props"
    Condition="Exists('$(MSBuildExtensionsPath)/$(MSBuildToolsVersion)/Microsoft.Common.props')"
    />

  <!-- Project Metadata -->
  <PropertyGroup>
    <RootNamespace>ChaosFrameworkBuild.ArchiveCreator</RootNamespace>
    <AssemblyName>ChaosFrameworkBuild.ArchiveCreator</AssemblyName>
    <ProjectGuid>{BBB52879-2D9B-419A-B6E0-679FB0A0EDC6}</ProjectGuid>
  </PropertyGroup>

  <!-- Properties -->
  <Import Project="submodules/ChaosPresets.Targets/Properties/Library.targets" />
  <Import Project="submodules/ChaosPresets.Targets/Paths/Output/bin.targets" />

  <!-- Configuration and Platform -->
  <Import Project="submodules/ChaosPresets.Targets/Configurations.targets" />
  <PropertyGroup Condition=" '$(Platform)' == 'AnyCPU' " />

  <!-- Included Files -->
  <ItemGroup>
    <Compile Include="source\**\*.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />

    <None Include="packages.config" />
  </ItemGroup>

  <!-- References -->
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Debug.targets"/>
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Platform.targets"/>
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosUtil.Reflection.targets"/>
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Collections.targets"/>
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.Core.targets"/>
  <Import Project="submodules/ChaosPresets.References/ProjectReferences/ChaosFramework.IO.targets"/>
  <ItemGroup>
    <Reference Include="System" />
  </ItemGroup>

  <!-- NuGet References -->
  <ItemGroup>
    <Reference Include="Microsoft.Build.Framework, Version=14.0.0.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a, processorArchitecture=MSIL">
      <HintPath>$(SolutionDir)packages\Microsoft.Build.Framework.14.3.0\lib\net45\Microsoft.Build.Framework.dll</HintPath>
      <Private>True</Private>
    </Reference>
    <Reference Include="Microsoft.Build.Utilities.Core, Version=14.0.0.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a, processorArchitecture=MSIL">
      <HintPath>$(SolutionDir)packages\Microsoft.Build.Utilities.Core.14.3.0\lib\net45\Microsoft.Build.Utilities.Core.dll</HintPath>
      <Private>True</Private>
    </Reference>
  </ItemGroup>

  <!-- Analyzers -->
  <Import Project="submodules/ChaosPresets.References/Analyzers/ChaosAnalyzers.CleanCode.targets" />

  <!-- Cleanup Task -->
  <Import Project="submodules/ChaosPresets.References/BuildTasks/ChaosBuild.CodeFormatter.Tasks.ChangedProjectFiles.targets" />

  <!-- Default Targets for Building CSharp Projects -->
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>